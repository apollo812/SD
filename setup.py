from setuptools import setup, find_packages

setup( 
	name='oridos_sdxl', 
	version='1.0', 
	description='', 
	author='', 
	author_email='', 
	packages=find_packages(), 
	install_requires=[ 
		'torch', 
	], 
) 
